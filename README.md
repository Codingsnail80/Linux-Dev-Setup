Simple script to install dev tools and libraries on debian based distros (eg. Debian, Ubuntu, Pop!_OS, etc.)
run this command to run the script once you have downloaded it: sudo chmod +x install.sh && sudo bash install.sh
